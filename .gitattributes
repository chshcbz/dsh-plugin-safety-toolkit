# Normalize line endings: LF in the repository, native on checkout.
* text=auto eol=lf

# Windows helper scripts should keep CRLF on Windows checkouts.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
